apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-storage
  labels:
    app.kubernetes.io/name: app
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
